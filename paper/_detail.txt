=== #2 GSNR: Graph-Smooth Null-Space Representation for Inverse Problems | arXiv 2602.20328 v2 (Apr 2026), cs.CV; Gualdron-Hurtado, Jacome, Suarez, Arguello (UIS Colombia)
METHOD: Uses RNSD x = x_r + x_n with P_r = H^dagger H, P_n = I - H^dagger H. Given a graph Laplacian L (4/8-NN pixel grid), forms the null-restricted Laplacian T = P_n L P_n and selects S as the p smoothest eigenmodes (lowest graph frequencies) of T as a projection basis for Null(H). Adds an optional null-only graph regularizer at inference. Two optimality criteria are formalized: COVERAGE (how much null-space variance p modes capture; Theorem 1 shows Laplacian spectra beat the flat spectrum of the bare projector P_n) and PREDICTABILITY (how well Sx can be linearly estimated from y; Proposition 1 gives per-mode R^2 rho_j^2 <= c_j/(c_j+mu_j) under a Gaussian prior). Theorem 2 proves the smoothest T-modes are minimax-optimal for null-space coverage.
KEY: Plugs into PnP, DIP, and diffusion solvers across deblurring, CS, demosaicing, SR. Reports up to +4.3 dB PSNR over baseline formulations and up to +1 dB over end-to-end learned models. Shows geometry-free (L=I, i.e., bare P_n) designs give only linear coverage growth (C_I(p)=p/q) and no minimax improvement (mu_{p+1}=1), whereas graph Laplacians achieve faster coverage and stronger statistical coupling to the measurements.

=== #4 NPN: Non-Linear Projections of the Null-Space for Imaging Inverse Problems | NeurIPS 2025 (arXiv 2510.01608 v2, Oct 2025); Jacome, Gualdron-Hurtado, Suarez, Arguello (UIS Colombia)
METHOD: Starting from RNSD x = P_r(x) + P_n(x) with P_r = H^dagger H, designs a projection matrix S (with rows in Null(H) via orthogonalization or analytic construction, dimension p <= n-m) and trains a network G(y) to predict the null-space coefficients Sx*. Reconstruction penalizes ||G(y) - S x_tilde||^2, concentrating regularization on the most informative unobserved directions rather than the whole image manifold. S and G are jointly optimized. Provides theoretical guarantees: a convergence-improvement zone (CIZ) for PnP whose size is controlled by the network's estimation error, and shows the NPN regularizer is minimized up to a constant depending on that error at the optimum. Complementary to conventional image-domain priors.
KEY: In a R^3 toy example, the learned G*(y) closely matches the true null-space projection Sx* on both train and OOD test points, while projecting a direct reconstruction into S gives large errors; OOD error grows only mildly for NPN vs drastic amplification for direct reconstruction. Consistently improves reconstruction fidelity across five inverse problems (CS, MRI, deblurring, SR, CT) with PnP, unrolling networks, DIP, and diffusion solvers.

=== #8 Computational Ghost Imaging (J. H. Shapiro) | arXiv:0807.2614 (2008); Phys. Rev. A 78, 061802(R)
METHOD: Starting from semiclassical (shot-noise) photodetection of pseudothermal light, Shapiro derives the cross-correlation C(rho1) = background C0 + ghost term C1(rho1) ~ integral exp(-|rho1-rho|^2/rho_L^2)|T(rho)|^2 via the Gaussian moment-factoring theorem, with resolution ~lambda0 L/a0. He then replaces the rotating ground glass with a deterministically driven SLM: because the field E2(rho,t) reaching the object is computable, the intensity pattern I1(rho1,t) that WOULD have hit the reference pinhole is precomputed by diffraction theory, and its mean-subtracted version DeltaI1-tilde is correlated with the single bucket photocurrent to form a background-free ghost image (Eq. 16). Only the object-arm bucket detector physically exists.
KEY: Yields background-free imagery in the narrowband limit (DC-block removes the C0 term), controllable resolution lambda0 L/D and field of view lambda0 L/d via SLM pixel geometry, and 3D sectioning (focal depth |DeltaL| <= k0 rho_L^2) by re-correlating the same bucket data against precomputed patterns at many ranges. Contrast ~1/N for binary objects. Because one beam and one detector are used, the image provably cannot arise from nonlocal two-photon interference — establishing GI's classical nature.

=== #9 Single-Pixel Imaging via Compressive Sampling (Duarte, Davenport, Takhar, Laska, Sun, Kelly, Baraniuk) | IEEE Signal Processing Magazine 25(2):83-91, March 2008
METHOD: A DMD spatial light modulator sets pixel-wise +-1 (0/1) test functions phi_m; a single photodetector integrates the inner product y[m]=<x,phi_m>, giving the underdetermined linear system y=Phi x=Phi Psi alpha with M<N. Since M<N there are infinitely many x' with Phi x'=y; CS theory says a random incoherent Phi lets sparse/compressible alpha be recovered by l1 basis pursuit (alpha-hat=argmin||alpha||_1 s.t. Phi Psi alpha=y) or, with noise, BPIC (||y-Phi Psi alpha||_2<epsilon), with M>=O(K log(N/K)). Reconstruction uses random Bernoulli/Walsh-Hadamard/noiselet rows and Total Variation minimization. The paper also analyzes multiplexing tradeoffs (dynamic range, quantization, Poisson noise) vs pixel-array/raster/basis scan, and the 'smashed filter' doing classification directly in R^M without reconstruction.
KEY: Reconstructed a 256x256 'R' from M=N/50 (50x sub-Nyquist) and a color Mandrill from M=N/10 under low light with a photomultiplier; CS MSE depends only logarithmically on N (M=O(K log(N/K))) vs linear for raster/basis scan; error bound ||alpha-hat - alpha||_2 < C_N epsilon + C_K sigma_K(x); smashed-filter classification uses manifold structure preserved under random projection (M=O(K log N)).

=== #10 Compressive Ghost Imaging | 2009, Applied Physics Letters (Katz, Bromberg, Silberberg; arXiv:0905.0321)
METHOD: Casts pseudothermal GI as a vector projection of the object transmission T(x,y) onto M random speckle patterns I_r(x,y), giving bucket values B_r = integral I_r T. Standard GI reconstructs by the linear cross-correlation T_GI = (1/M) sum (B_r - <B>) I_r, requiring M >= N (and in practice M >> N for SNR > 1). The paper instead solves a CS program: find T' minimizing ||Psi{T'}||_L1 (Psi = 2D-DCT) subject to integral I_r T' = B_r for all r (relaxed to inequality under noise, solved via GPSR). Because the random speckle patterns are incoherent/near-orthogonal, K-sparse images are recovered from M ~ O(K log(N/K)) sub-Nyquist measurements. Applicable to any past pseudothermal GI dataset since it only changes the reconstruction step.
KEY: On a double-slit plate, CS gives ~4.4x higher SNR at 256 realizations (15% of the N=1750 Nyquist limit) and ~4x at 512 (30%); MSE 0.04 (CS) vs 0.12 (GI) at 512 realizations. Hebrew-letter Aleph: 3.5x SNR at 1024 measurements, MSE 0.05 vs 0.1. Grayscale Nyquist portrait: MSE 0.005 (CS) vs 0.09 (GI) from 800 (60% Nyquist) measurements. SNR scaling as sqrt(N) implies ~order-of-magnitude faster acquisition.

=== #11 Ghost Imaging with a Single Detector (Computational Ghost Imaging, experimental) | 2009, Physical Review A (Bromberg, Katz, Silberberg; arXiv:0812.2633)
METHOD: An SLM imposes known pseudo-random phase masks phi_r(x,y) on a HeNe beam; the field at any plane z is computed via the Fresnel-Huygens propagator E_r(x,y,z), so I_r=|E_r(x,y,L)|^2 is calculated rather than measured. Only a single bucket detector records B_r = integral I_r(x,y,L) T(x,y). The image is G(x,y)=(1/N) sum (B_r-<B>) I_r = <BI> - <B><I>, i.e. the object convolved with the field coherence function; resolution is set by the speckle/coherence size (delta_x = lambda z / pi w0). Because the reference arm is virtual, reconstruction at any z is pure post-processing, giving scan-free depth sectioning; swapping the bucket for a pinhole and Fourier-transforming the field yields ghost diffraction from the same data.
KEY: 16000 realizations reconstruct a 2cm x 2cm mask; measured SNR follows the theoretical sqrt(N) law (SNR proportional to sqrt(N/N_s)). Depth resolution delta_z(L=84cm) ~ 50cm; out-of-focus imaging at other z-planes demonstrated. Simultaneous GI + ghost diffraction of a double slit from 8000 realizations gives near/far-field resolution product delta_x*delta_k = 0.025 << 0.5 (Fourier limit) without violating the classical EPR bound.

=== #13 Single-Pixel Imaging 12 Years On: A Review | 2020, Optics Express 28(19):28190 (Gibson, Johnson, Padgett, University of Glasgow)
METHOD: Frames single-pixel imaging as filtering a scene with M mask patterns and recording correlated single-pixel intensities; basic reconstruction O_(x,y),M = (1/M) sum S_m P_(x,y),m needs M=N orthogonal patterns (or M>>N for non-orthogonal/noisy sets). Distinguishes structured detection (single-pixel cameras) from structured illumination (computational GI). Reviews regularized reconstruction via cost C = chi^2/M(O) + lambda R(O), where lambda balances data-fidelity against a prior; M>N -> denoising, M<N -> compressed sensing (l1 sparsity in wavelet/DCT bases). Section 8 covers the CNN/deep-learning approach: CNNs both learn an efficient sampling basis and act as the reconstruction map, and enable image-free classification.
KEY: Notes that Higham et al. deep-learned a sampling basis achieving 4% compression (666 of 16384 patterns) for real-time 128x128 2D and 3D video reconstruction; CNN reconstructions match regularizer methods but with far fewer, learned patterns. Emphasizes non-visible-wavelength and time-of-flight/LiDAR 3D advantages; timeline of the field's key papers (Sen 2005, Candes 2006, Duarte 2008, Shapiro 2008, Katz/Bromberg 2009, Ferri 2010, Sun 2013, Higham 2018, Radwell 2019).

=== #16 Equivariant Imaging: Learning Beyond the Range Space | ICCV 2021 (Chen, Tachella & Davies)
METHOD: Formalizes the impossibility: Proposition 1 - any f(y) = A^dagger y + v(y) with image(v) in null(A) satisfies A f(y) = y, so infinitely many measurement-consistent reconstructions exist and MC gives no information on the null-space component. To escape, assume signal set X is invariant to a group G of unitary transforms T_g; then each y = A x = (A T_g) T_g^T x is a virtual operator A_g = A T_g whose range is the rotated range T_g^T R_A. Theorem 1: recovering X requires the stacked operator M = [A T_1; ...; A T_|G|] to have rank n (so m|G| >= n); Corollary 1: R_A must NOT be invariant to G. Training loss = measurement-consistency + alpha * equivariance term ||T_g f(y) - f(A T_g f(y))||, with an optional adversarial invariant-distribution term. Uses f = G_theta o A^dagger with residual U-Net.
KEY: On 50-view sparse CT (CT100), EI reaches 36.94 dB vs supervised 38.17 dB and MC-only 31.01 dB (~7 dB over FBP); on 30%-drop inpainting (Urban100) EI 25.14 dB vs A^dagger y / MC ~5.84 dB (~20 dB gain), within 1.37 dB of supervised. EI also shows smaller train/test generalization gap than supervised. Shift-invariance is useless for Fourier-type operators (deblur/SR/MRI) since A^T is shift-invariant (Corollary 1).

=== #17 Robust Equivariant Imaging (REI): learning to image from noisy and partial measurements | CVPR 2022 (Chen, Tachella & Davies)
METHOD: Vanilla EI degrades rapidly with measurement noise. REI keeps the equivariance idea (invariant signal set gives virtual operators A_g = A o T_g, enforce f o A equivariant) but makes both loss terms noise-robust: L_REI = L_SURE + alpha L_REQ. L_SURE is a Stein's Unbiased Risk Estimator of the (unknown) clean measurement-consistency MSE, using Monte-Carlo divergence estimation (single random probe b, div h ~ (1/tau) b^T (h(y+tau b) - h(y))). Noise-specific SURE forms are derived for Gaussian, Poisson (Bernoulli probe), and mixed Poisson-Gaussian. L_REQ enforces equivariance while accounting for noise on the virtual measurements. f = G_theta o A^dagger, architecture-agnostic.
KEY: On 4x accelerated MRI (fastMRI), REI beats EI by 3.6 dB at sigma=0.1 and ~7 dB at sigma=0.2, staying within 1.5-2.5 dB of supervised across noise levels (e.g. sigma=0.2: REI 26.9 vs EI 19.7 vs Sup 28.8 dB). Poisson inpainting: REI beats EI by >8 dB at high noise, within 1 dB of supervised. Nonlinear sparse-view CT (MPG noise): REI ~5 dB over EI, near supervised (34.0 vs 29.2 vs 34.4 dB). Oracle-REI (true clean MC) confirms SURE closely approximates clean consistency.

=== #18 Unsupervised Learning From Incomplete Measurements for Inverse Problems | NeurIPS 2022 (Tachella, Chen & Davies)
METHOD: Separates two questions: Q1 signal recovery (unique x for given y) and Q2 model identification (unique support X from measurements alone). With a single fixed incomplete A there is no null-space information, so learning is impossible; using G operators with different null-spaces each constrains X via X_hat_g = {x_hat_g + u : u in null(A_g)}, and the inferred set is the intersection X_hat = ∩_g X_hat_g. Uses box-counting dimension k of the signal set and the normalized secant set. Necessary condition: rank[A_1;...;A_G] = n hence m >= n/G (Prop 4.1, = Thm 1 of EI). Sufficient condition (Thm 4.2, embedology/Sauer-Yorke argument): for almost every G operators, m > k + n/G identifies X; extends to noisy data if the noise characteristic function is nowhere zero (Gaussian). Proposes MOI loss: measurement consistency ||y - A_g f(y,A_g)||^2 + cross-operator consistency ||x_hat - f(A_s x_hat, A_s)||^2 preventing the trivial pseudo-inverse.
KEY: MNIST CS (box-dim k~12): reconstruction PSNR jumps exactly when m > k + n/G, matching the theoretical red-line boundary; below it, performance equals the pseudo-inverse. Subspace-learning Monte-Carlo phase transition follows m > k + n/G. CelebA inpainting: MOI 34.05 dB, +4 dB over AmbientGAN, 2.1 dB below supervised. 4x fastMRI (unrolled PGD-3): MOI 31.39 dB beats measurement-splitting 29.47 dB, near supervised 32.42 dB.

=== #20 On instabilities of deep learning in image reconstruction - Does AI come at a cost? | Antun, Renna, Poon, Adcock, Hansen — PNAS 2020 (arXiv 2019)
METHOD: Given an undersampled forward model Ax=y (A in C^{m x N}, m<N) and a trained reconstruction net f, the authors search for worst-case perturbations via gradient ascent on an unconstrained Lasso-style objective Q(r)=1/2||f(y+Ar)-p(x)||^2 - lambda/2||r||^2 (Algorithm 1; Algorithm 2 replaces A* by a filtered-backprojection operator for Radon). They test six networks (AUTOMAP, DAGAN, Deep MRI, Ell-50, Med-50, MRI-VN) across Fourier and Radon sampling. Every SoA comparison uses a provably-stable compressed-sensing/shearlet+TGV benchmark to prove the instability is intrinsic to the net, not ill-conditioning of the problem. Three failure modes: (1) near-invisible image/measurement perturbations produce severe but physically-plausible artefacts; (2) small structural changes (card symbols, letters, an inserted heart/tumour) vanish in the reconstruction; (3) more samples can worsen PSNR.
KEY: Deep MRI is highly unstable to tiny perturbations yet stable to structural changes; Ell-50 removes inserted structural detail entirely; Ell-50/Med-50/DAGAN show PSNR DECAY with increasing subsampling ratio (counterintuitive mode 3); the stable CS benchmark is unaffected by the same perturbations. Networks stable in one category are often unstable in the other. Uses 33%/60%/20%/15% subsampling settings; instability appears across radically different architectures and both Fourier and Radon operators.

=== #29 Learned Primal-Dual Reconstruction | 2018, IEEE Transactions on Medical Imaging (arXiv:1707.06474); Adler & Oktem (KTH / Elekta)
METHOD: For an inverse problem g = T(f_true) + noise, they unroll the Chambolle-Pock/PDHG algorithm (which alternates dual and primal proximal updates connected by the forward operator T and adjoint-of-derivative [dT]^*). The proximal operators prox_F, prox_G are replaced by learned residual CNNs (Id + convs + PReLU); primal and dual spaces are extended to N=5 'memory' channels; over-relaxation and evaluation points are learned rather than hard-coded; and different learned proximals are used per iteration (I=10). Forward operator is embedded as a TensorFlow/ODL layer so gradients flow through T and its adjoint; trained end-to-end on MSE. Reduces to classical PDHG / gradient descent for specific parameter choices (universal-approximation argument).
KEY: On low-dose CT: Shepp-Logan/ellipse phantoms, Learned Primal-Dual beats FBP, TV, learned gradient, and FBP+U-Net post-processing by >6 dB PSNR (38.28 dB, SSIM 0.989). On human phantoms, +10.5 dB over FBP, +6.6 dB over TV, +2.2 dB over U-Net denoising (44.11 dB, SSIM 0.969) with clinically feasible ~620 ms runtime, only 2.4e5 parameters, and no initial reconstruction needed. Crucially, Fig. 4 states that among all methods ONLY Learned Primal-Dual recovers the true small feature, while in the others 'the true feature is indistinguishable from other false features of same size/contrast' and a str

=== #35 Compressed Sensing using Generative Models (CSGM) | 2017, ICML (Bora, Jalal, Price, Dimakis)
METHOD: Given y = Ax* + eta with m<<n, minimize loss(z)=||AG(z)-y||^2 over the latent z of a pretrained VAE/GAN generator G:R^k->R^n by gradient descent (nonconvex but empirically effective), optionally with an lambda||z||^2 likelihood regularizer. The recovery guarantee rests on the Set-Restricted Eigenvalue Condition (S-REC): A satisfies S-REC(S,gamma,delta) if ||A(x1-x2)||>=gamma||x1-x2||-delta for all x1,x2 in S. The core geometric requirement is that differences of any two 'natural' vectors stay far from the null space of A. Random Gaussian A of m=O(kd log n) rows satisfies S-REC for d-layer ReLU generators w.h.p., yielding the recovery theorem.
KEY: Theorem 1.1: for a d-layer ReLU generator and m=O(kd log n) Gaussian measurements, ||G(z_hat)-x*|| <= 6 min_z ||G(z)-x*|| + 3||eta|| + 2eps with prob 1-e^{-Omega(m)}. Theorem 1.2 gives the L-Lipschitz analog with m=O(k log(Lr/delta)). Empirically 5-10x fewer measurements than Lasso for equal accuracy on MNIST (VAE) and CelebA (DCGAN). Error is bounded by an irreducible representation-error floor min_z||G(z)-x*|| plus noise.

=== #50 The Perception-Distortion Tradeoff | CVPR 2018 (extended IEEE T-PAMI 2020 version); Blau & Michaeli
METHOD: Defines distortion as E[Delta(X,Xhat)] for any full-reference measure and perceptual quality as a divergence d(pX, p_Xhat) (linked to optimal real-vs-fake discrimination: p_success = 0.5*dTV(pX,p_Xhat)+0.5). Introduces the perception-distortion function P(D)=min d(pX,p_Xhat) s.t. E[Delta]<=D. Theorem 2: P(D) is monotonically non-increasing and convex for any convex divergence and ANY distortion measure. Theorem 1: for non-invertible degradations no distortion metric is 'stably distribution preserving.' Theorem 3: for squared error, perfect perceptual quality costs at most Dmax<=2*Dmin (i.e. <=3 dB PSNR), attained by sampling xhat from the posterior p_{X|Y}. Shows GANs with loss = distortion + lambda*adversarial traverse the bound as lambda (a Lagrange multiplier) varies.
KEY: Tradeoff is convex and steepest at low distortion / high perceptual quality. Perfect perceptual quality costs at most a factor-2 MSE increase (3 dB PSNR). Empirically SR methods with lowest perceptual index have RMSE ~sqrt(2)x larger than lowest-RMSE methods. 16 SR algorithms plotted on the perception-distortion plane over RMSE/SSIM/MS-SSIM/IFC/VIF/VGG show a blank (unattainable) lower-left corner for every distortion metric; FR and NR metrics anti-correlate near the bound.

=== #84 Robust Uncertainty Principles: Exact Signal Reconstruction from Highly Incomplete Frequency Information | 2004/2006 (arXiv:math/0409186; IEEE Trans. Information Theory, Candes-Romberg-Tao)
METHOD: For f in C^N observed only on a random frequency set Omega (|Omega| << N), solve min ||g||_1 s.t. g-hat(omega)=f-hat(omega) for all omega in Omega (or min TV for images). Main theorem: if the support |T| <= alpha(M)(log N)^{-1}|Omega|, then with probability >= 1 - O(N^{-M}) the L1 solution equals f exactly. A deterministic companion (Theorem 1.1, N prime): the restricted Fourier transform F_{T,Omega} is a bijection when |T|=|Omega|, so if |T| <= |Omega|/2 the signal is UNIQUELY determined from f-hat|Omega; CONVERSELY, whenever Omega is not all frequencies, there exist DISTINCT f,g with |supp f|,|supp g| <= |Omega|/2 + 1 and f-hat|Omega = g-hat|Omega — i.e. two different signals producing identical measurements. The star-shaped-sampling Logan-Shepp phantom is reconstructed as an EXACT replica by TV minimization, whereas zero-filling (minimum-energy = A^dagger y) yields severe artifacts.
KEY: Exact recovery of the Shepp-Logan phantom from ~22 radial lines by TV minimization (Fig 1d), vs. severe nonlocal artifacts from minimum-energy zero-fill (Fig 1c). Recovery guarantee sharp up to the (log N) factor. Explicit uniqueness/non-uniqueness threshold at |Omega|/2.

=== #87 Deep-learning-based ghost imaging (GIDL) | 2017, Scientific Reports 7:17865 (Lyu, Wang, Wang, Wang, Li, Chen, Situ)
METHOD: Ghost imaging illuminates object T(x) with M speckle patterns I_m(x); a bucket detector records S_m = integral I_m(x) T(x) dx. Step 1: reconstruct a noisy image O via traditional correlation O(x) = <delta S_m * delta I_m(x)>. Step 2: a fully-connected DNN (input reshape 32x32->1024, three 1024-unit ReLU hidden layers, output reshape) trained by MSE/SGD (Adam in experiments) on 2000 MNIST digit pairs (noisy-GI reconstruction, ground truth) learns to denoise/complete O into T. Sampling ratio beta = M/N_spec (patterns per speckle count); at beta<1 the operator is undersampled. Compared against CSGI, which instead solves a TV-regularized inverse problem min ||grad T||_1 + (u/2)||A T - S||^2 (TVAL3 solver).
KEY: GIDL reaches MSE ~0.03 vs ground truth even at beta=0.1, and is 'not very sensitive to beta.' Outperforms CSGI at extremely low sampling (recognizable digits at beta=0.05 where CSGI is fully corrupted) and under high additive Gaussian detector noise (w=50), where CSGI fails. Trade-offs noted: over-fitting past ~500 epochs introduces bit-error black spots; at very low beta + high noise the DNN output is 'blurred, distorting the feature of the object' and 'does not resemble exactly the ground truths.' Optical experiment with SLM speckle illumination confirms simulation.

=== #90 Ghost Imaging Based on Deep Learning | 2018, Scientific Reports 8:6469
METHOD: Ghost imaging is posed as y=Ax with A the DMD speckle pattern matrix and a bucket detector giving scalar echo signals. A rudimentary image is first reconstructed by FISTA compressed sensing at low sampling rate (Eq. 2, TV/sparsity regularized), then fed into a modified CNN (two conv + two pool + one linear layer, ramp activation, NMSE loss) whose linear output is reshaped into the target image. The network is trained on 100-500 samples per scene (aircraft, cat, Lena) with full-sampling CCD labels; it learns a scene-specific input-to-label mapping rather than enforcing measurement consistency.
KEY: At 20% sampling the DLGI reaches PSNR 26.30 dB in 0.0023 s vs 26.62 dB / 0.31 s for basic correlation and 26.52 dB / 5.8 s for CS (a ~130x-2500x reconstruction speedup at matched quality). PSNR degrades gracefully to 23.2 dB at 5%. DLGI stays significantly above CS and correlation across measurement SNR from 0 to -20 dB. Complex scenes (Lena) need more training data (500->1000 samples raises PSNR only ~1 dB, still ~16 dB) and reconstruction is confined to trained target classes.

=== #91 Deep Learning in Classical X-ray Ghost Imaging for Dose Reduction | 2024, arXiv:2411.06340 (physics.optics)
METHOD: Two-dimensional GI is modeled as bucket signals b_n = sum In(x,y)T(x,y) with adjoint recovery T_hat = sum In (b_n - <b>). Four mask families (random binary, Hadamard, Hartley, PCA) are compared; PCA masks (data-driven orthogonal basis via SVD X=U Sigma V^T) reconstruct MNIST digits at only beta=2.55%. An end-to-end network has a Front-end whose first fully-connected layer weights are manually set to the GI adjoint (scaled by the GI gain gamma^-1, gamma=B sigma_I^2) plus a U-Net Back-end denoiser. Poisson (shot) + Gaussian (electronic) noise are simulated to tie sampling to actual x-ray dose.
KEY: DLGI reconstructs digits down to beta=1.28% (B=10 masks). But under matched total dose (~3810 photons) and equal detector quantum efficiency, back-end-DENOISED direct imaging beats DLGI: classification accuracy 65% (DLGI) vs 75% (DI) on digits, ~68% for both on Fashion-MNIST; DLGI loses fine detail (stripes/letters). Authors conclude it is 'very challenging for DLGI to outperform DI under extremely low-dose conditions' given the same prior; GI's only real edge is a possible 35-160% flux gain from a high-DQE bucket detector. Explicitly flags no out-of-distribution robustness (cannot train on he

=== #93 Necessary Density Conditions for Sampling and Interpolation of Certain Entire Functions | 1967, Acta Mathematica 117:37-52; H. J. Landau (Bell Labs)
METHOD: For B(S), the space of L2 functions with Fourier support on a finite union of intervals (or bounded measurable S in R^N), Landau connects sets of sampling and interpolation to a concentration eigenvalue problem (the operator D_Q B_S formed from spatial and spectral truncations) rather than complex-variable methods. Theorem 1: a sampling set A must satisfy n^-(r) >= (2pi)^-1 m(S) r - A log+ r - B; Theorem 2: an interpolation set satisfies n^+(r) <= (2pi)^-1 m(S) r + A log+ r + B. The measure of the spectrum m(S) thus sets a hard density floor/ceiling independent of dimension, proving Beurling's balayage conjecture.
KEY: The number of independent samples per unit length/area needed to determine a bandlimited signal cannot fall below m(S)/2pi (the Nyquist/Landau rate); below it, stable sampling is impossible. Sampling sets are shown to differ fundamentally from mere sets of uniqueness (whose density can be unrelated to m(S)). This is the rigorous origin of the 'you can measure at most m independent modes' principle underlying the eigenvalue/prolate-function analysis of degrees of freedom.

=== #97 Advances and Challenges of Single-Pixel Imaging Based on Deep Learning | 2025, Laser & Photonics Reviews 19(7):2401397 (review)
METHOD: States the SPI forward model B = H T + eta (B in R^{m x1} measurements, H in R^{m x n} modulation matrix of m patterns each n pixels, T the scene) and notes SPI and ghost imaging are now considered theoretically congruent. Reviews two hardware setups (structured detection vs structured illumination) and reconstruction families: non-iterative (inverse-transform, second-order correlation), iterative/compressive-sensing, and deep learning (post-processing of low-quality reconstructions, direct measurement-to-image mapping, and image-free sensing/classification). Surveys DL sub-domains and the network zoo (CNN/U-Net/GAN/transformer). Emphasizes DL delivers higher quality and faster reconstruction at low sampling rates but depends heavily on large high-quality training data.
KEY: SPI spectral range extended from visible to UV/IR/THz. Image-free sensing example reaches 88.4% average precision at an extremely low sampling rate of 0.015 while cutting inference from 4.0s to 0.1s. Challenges section names DL's core liabilities: data hunger, limited interpretability, overfitting, and limited generalization, plus an inherent image-quality vs speed trade-off; proposes better forward physics models, composite/simplified learning, and pruning as mitigations.
