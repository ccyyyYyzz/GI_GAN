run_name: ga_nsgan_smoke
output_dir: outputs/compatibility/gan_high_quality_gi/ga_nsgan/smoke
device: cuda
seed: 20260625

protocol:
  status: smoke_gradient_audit_not_for_claims
  old_matched_package: outputs/compatibility/gan_high_quality_gi/GAN_HQ_MATCHED_CAUSAL_VALIDATION_PACKAGE.zip

data:
  dataset_root: E:/ns_mc_gan_gi/data
  dataset_name: stl10
  source_split: train+unlabeled
  img_size: 64
  train_offset: 0
  train_count: 256
  val_offset: 256
  val_count: 64
  dev_offset: 320
  dev_count: 64
  batch_size: 8
  num_workers: 0

operator:
  sampling_ratio: 0.05
  total_m: 205
  dct_rows: 128
  hadamard_rows: 56
  random_rows: 20
  seed: 772001
  noise_std: 0.0
  lambda_solver: 1.0e-4
  lmmse_lambda: 1.0e-4

model:
  model_type: hq_unet
  base_channels: 32
  discriminator: gauge_aligned_residual_wavelet
  ema_decay: 0.995

matched_training:
  train_seeds: [0]
  pretrain_steps: 4
  branch_steps: 3
  equivalence_seed: 991
  equivalence_steps: 2
  pretrain_shuffle_seed_offset: 210000
  branch_shuffle_seed_offset: 310000
  discriminator_seed_offset: 410000

training:
  lr_g: 0.0002
  lr_d: 0.0002
  betas: [0.5, 0.9]
  amp: true
  grad_clip: 1.0
  lambda_charb: 10.0
  lambda_l1: 10.0
  lambda_lpips_train: 2.0
  lambda_grad: 2.0
  lambda_spec: 0.5
  lambda_fm: 0.5
  r1_gamma: 0.0

ga_nsgan:
  disc_base_channels: 24
  lambda_ns_spatial: 1.0
  lambda_wavelet: 1.0
  lambda_img: 0.15
  lambda_fm: 0.5
  target_grad_ratio: 0.2
  lambda_adv_min: 0.0005
  lambda_adv_max: 0.5
  gradient_audit_every: 1

eval:
  lpips: true
  kid_fid: true
  kid_fid_max_images: 48
  qualitative_count: 8
  bootstrap_replicates: 200
  bootstrap_seed: 20260626
  psnr_drop_tolerance_db: 0.5
  relmeaserr_limit: 1.0e-5
  lpips_relative_gain_gate: 0.05
