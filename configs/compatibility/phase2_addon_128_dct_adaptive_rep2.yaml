run_name: phase2_addon_128_dct_adaptive_rep2
output_dir: outputs/compatibility/phase2_addon_128_dct_adaptive_rep2/dev_addon_128_rep2
device: cuda
batch_size: 8
candidate_k: 16
candidate_seed: 960700
checkpoint: E:/ns_mc_gan_gi/outputs_phase79_posterior_anti_collapse/rad5_rowspace_diversity_diagnostic/checkpoints/final.pt

context_operator:
  status: fresh_development_operator_not_locked_test
  pattern_type: rademacher
  sampling_ratio: 0.05
  seed: 96051
  noise_std: 0.0
  matrix_normalization: legacy_sqrt_m

split:
  name: phase2_addon_128_rep2_dev
  source: STL10 train+unlabeled
  train_unlabeled_offset: 7168
  sample_count: 128
  status: development_only_not_final

witness:
  experiment_family: add_on_witness_dct_adaptive_rep2
  operator: fresh_rows_not_seen_by_generator
  budgets: [24, 32, 64]
  random_witness: rademacher
  random_seed: 96101
  fixed_witness: dct2_low_frequency
  adaptive_library: dct2_low_frequency
  adaptive_library_seed: 96102
  adaptive_library_size: 256
  primary_selector: dm_fcc_seed3
  compatibility_prefilter_top_m: 4

statistics:
  bootstrap_replicates: 2000
  bootstrap_seed: 96110
  primary_unit: image

quality:
  compute_lpips: true
  lpips_device: cuda

pilot_gate:
  scope: independent_larger_fresh_development_add_on_replicate
  primary_budget: 64
  low_budget: 24
  min_oracle_gain_fraction: 0.45
  decision_rule: >
    This is an independent development replicate, not a locked test. It checks
    whether DCT-library adaptive add-on witness repeats the 128-sample signal
    on a new fresh operator and a disjoint train+unlabeled slice.
