seed: 42
device: cuda
img_size: 64
dataset_root: E:/ns_mc_gan_gi/data
output_dir: E:/ns_mc_gan_gi/outputs_phase5/fixed_0p5pct
batch_size: 8
num_workers: 0
epochs: 10
lr_g: 0.0001
lr_d: 0.0001
betas: [0.5, 0.9]
sampling_ratio: 0.005
pattern_type: rademacher
use_learned_patterns: false
noise_std: 0.01
lambda_solver: 0.001
lambda_l1: 100.0
lambda_dc_loss: 10.0
lambda_tv: 0.001
lambda_adv: 0.01
lambda_gp: 10.0
n_critic: 3
save_every: 1
eval_every: 1
save_image_every: 250
limit_train_samples: 5000
limit_val_samples: 500
use_null_project: true
use_dc_project: true
use_adversarial: true
num_eval_samples_to_save: 8
score_ssim_weight: 10.0
checkpoint_metric_mode: score
eval_before_training: true
